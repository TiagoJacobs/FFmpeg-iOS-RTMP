version: 2
jobs:
   build:
     macos:
       xcode: "9.3"
     steps:
       - checkout

       - run: sh ./build-libssl.sh

       - run: man lipo

       - run: sh ./build-librtmp.sh

       - store_artifacts:
          path: ~/libssl.tar.gz
          destination: libssl.tar.gz
